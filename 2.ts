(() => {



function greet(person: string): string {
    return "Hello, " + person;
}

const userName = 'Galadriel'; 
// const userName = {firstName: "Galadriel"};

console.log( greet(userName) );


// Slides
})();